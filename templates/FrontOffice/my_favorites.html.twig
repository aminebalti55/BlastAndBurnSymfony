{% extends '/FrontOffice/base.html.twig' %}

{% block body %}
    <!-- Hero Area Start-->
    <div class="slider-area ">
        <div class="single-slider slider-height2 d-flex align-items-center">
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="hero-cap text-center">
                            <h2>My favorites</h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Hero Area End-->
    <section class="property-section latest-property-section spad">
        <div class="container">

            <div class="row">
                <div class="col-lg-5">
                    <div class="section-title">
                        <h4>My favorites</h4>
                    </div>
                </div>
                <div class="col-lg-7">
                    <div class="property-controls">
                        <ul>
                            <li data-filter="all">All</li>
                            <li data-filter=".event">Events</li>
                            <li data-filter=".session">sessions</li>
                            <li data-filter=".recipe">Recipes</li>
                        </ul>
                    </div>
                </div>
            </div>

            <div class="row property-filter">
                {% set classType="col-lg-4 col-md-6 mix all event" %}
                {% set img='assets/img/portfolio/portfolio-3.jpg' %}
                {% set title='' %}
                {% set id=0 %}
                {% for all in myfav %}
                   
                    {% if all.type=='event' %}
                        {% set id=all.event.id %}
                        {% set title=all.event.title %}
                        {% set classType="col-lg-4 col-md-6 mix all event" %}
                        {% set img='assets/img/event.jpg' %}
                    {% elseif all.type=='session' %}
                        {% set id=all.session.id %}
                        {% set title=all.session.title %}
                        {% set classType="col-lg-4 col-md-6 mix all session" %}
                        {% set img='assets/img/session.jpg' %}
                  
                    {% elseif all.type=='recipe' %}
                        {% set id=all.recipe.id %}
                        {% set title=all.recipe.title %}
                        {% set classType="col-lg-4 col-md-6 mix all recipe" %}
                        {% set img='assets/img/recipe1.jpg' %}
                    {% endif %}
                    <div class="{{ classType }}">
                        <div class="property-item">
                            <div class="pi-pic set-bg" data-setbg="../assets/others/img/property/property-1.jpg">
                                <div class="label">{{ all.type }}</div>
                            </div>

                            <div class="pi-text">
                                <h5><a href="{{ path ('details', {'type':all.type|lower, 'id':id}) }}">{{ title }}</a></h5>
                                
                                <div class="pi-agent">
                                    <div class="pa-item">
                                        <div class="pa-info">
                                            <img src="../assets/others/img/property/posted-by/pb-1.jpg" alt="">
                                            <h6>{{all.user.firstName}}  {{all.user.lastName}}</h6>
                                        </div>
                                        <div class="pa-text">
                                        {{all.user.email}}                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}

            </div>

        </div>
    </section><!-- End Portfolio Section -->
{% endblock %}
